# Use the super-fast CDN available with CocoaPods 1.7+
# gem install cocoapods --pre
source 'https://cdn.jsdelivr.net/cocoa/'

platform :ios, '10.0'

inhibit_all_warnings!

target 'SlideKitTestApp' do
  use_frameworks!
  pod 'GSTouchesShowingWindow'
end
